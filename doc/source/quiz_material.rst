.. code-block:: python3

    class a:
        def __init__(self, b):
            self.b = b

        def c(self):
            return float(self.b)


    d = a
    e = a(2)
    f = e.c()
    g = e.c


.. code-block:: python3

    False if a and b else True

.. code-block:: python3

    not (a and b)

.. code-block:: python3

   not a and b

.. code-block:: python3

   not a or not b

.. code-block:: python3

   a and b

.. code-block:: python3

   (not a) and b

`not` has `and`

`integrate`

`IntegrationMeasure`

`__hash__`

`_prepare_integration`

`problemDomain`

`Lebesgue_Measure`

